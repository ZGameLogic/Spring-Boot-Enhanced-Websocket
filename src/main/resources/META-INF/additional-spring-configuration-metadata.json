{
  "properties": [{
      "name": "websocket.endpoint",
      "type": "java.lang.String",
      "description": "The endpoint to listen on for websocket connections."
  }],
  "hints": [{
    "name": "websocket.endpoint",
    "values": [{
      "value": "ws"
    }]
  }]
}
